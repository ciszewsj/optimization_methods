
clear all
close all
clc
load('Data01.mat')
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

n = length(y);
tau = 2;
D = spdiags([ones(n-1,1) -ones(n-1,1)], [0 1], n-1, n);
q = 2;
m = 0;
n = 0;

A = [
        [eye(n), -eye(n), zeros(n,m)],
        [-eye(n),-eye(n), zeros(n,m)]
    ];
c = [zeros(n, 1), ones(n, 1), zeros(m, 1)];

